"""Pluggable text sources, one per file format.

See `base.py` for the `TextSource` protocol and `docs/design.md` §4 for
architecture. The `SOURCES` registry is populated by individual source
modules during implementation.
"""
